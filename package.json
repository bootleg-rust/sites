{
  "name": "@bootleg-rust/sites",
  "version": "0.0.1",
  "private": true,
  "description": "monorepo containing `bootleg` versions of rust-lang websites",
  "author": "pseudo-su",
  "scripts": {
    "dev": "echo 'TODO: start all the dev hot reload servers'",
    "package": "echo 'TODO: rushx package in each repo'",
    "publish": "echo 'TODO: rushx publish in each repo '",
    "deploy": "echo 'TODO: rushx deploy in each repo '",
    "test": "echo 'TODO: rushx test in each repo'",
    "verify": "echo 'TODO: rushx verify in each repo'"
  },
  "engines": {
    "node": "12.16.3"
  }
}
