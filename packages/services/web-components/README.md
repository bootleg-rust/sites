# Components Library (@bootleg-rust/web-components)

WIP: docs

```sh
docker build --progress=plain -t web-components -f Dockerfile ../..;
docker run -it --init -p 8080:8080 \
  -e ENV=dev \
  web-components;
```
