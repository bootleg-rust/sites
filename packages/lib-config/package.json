{
  "name": "@bootleg-rust/lib-config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared config between different sites",
  "author": "pseudo-su",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint --max-warnings=0 .",
    "lint:fix": "eslint --max-warnings=0 --fix .",
    "test": "jest"
  },
  "dependencies": {
    "@bootleg-rust/lib-ssr-toolbox": "*",
    "envalid": "^6.0.1"
  },
  "devDependencies": {
    "@bootleg-rust/build-tooling": "*",
    "@jest/globals": "^26.6.2"
  },
  "engines": {
    "node": "12.16.3"
  }
}
