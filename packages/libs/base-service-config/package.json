{
  "name": "@bootleg-rust/base-service-config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared config between different sites",
  "author": "pseudo-su",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo \"Nothing to build\"",
    "test": "jest",
    "verify": "eslint --max-warnings=0 .",
    "verify:fix": "eslint --max-warnings=0 --fix ."
  },
  "dependencies": {
    "@ssr-kit/toolbox": "*",
    "envalid": "^6.0.1"
  },
  "devDependencies": {
    "@bootleg-rust/build-tooling": "*",
    "@jest/globals": "^26.6.2",
    "jest-cli": "^26.6.3"
  },
  "engines": {
    "node": "14.15",
    "npm": "DO_NOT_USE",
    "pnpm": ">=4.14.4",
    "yarn": "DO_NOT_USE"
  }
}
