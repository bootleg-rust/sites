# lib-cli

Examples:

```sh
yarn build-tooling-cli trigger ci deploy:all --git-ref=master

yarn build-tooling-cli notify deployment

yarn build-tooling-cli service deploy --service-name web-crates-io
yarn build-tooling-cli service package --service-name web-admin
yarn build-tooling-cli service publish --service-name web-admin
```
